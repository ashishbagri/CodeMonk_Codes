package dayZero;

import java.util.Scanner;

public class MicroHouse {
	private static long[][] rect;
	
	public static void main(String[] args) {
		System.out.println(1^2^4^5^7^8);
		System.out.println(2^3^5^6^8^9);
		Scanner sc = new Scanner(System.in);
		int n = Integer.parseInt(sc.next());
		int m = Integer.parseInt(sc.next());
		rect = new long[n][m];
		for(int i=0;i<n;i++){
			for(int j=0;j<m;j++)
				rect[i][j]= Long.parseLong(sc.next());
		}
		findMaxLuck(n,m);
	}

	private static void findMaxLuck(int n, int m) {
		long[] temp = new long[n];
		long max =0;
		for(int left=0;left<m;left++){
			for(int i=0; i < n; i++){
                temp[i] = 0;
            }
			for(int right=left;right<m;right++){
				for(int i=0;i<n;i++){
					temp[i]^=rect[i][right];
				}
				long luck = findLuck(temp);
				if(luck>max){
					max=luck;
				}
			}
			
		}
		System.out.println(max);
		
	}

	private static int findLuck(long[] temp) {
		int luck =0;
		for(int i=0;i<temp.length;i++){
			luck^=temp[i];
		}
		return luck;
	}
}
